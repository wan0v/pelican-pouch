<?xml version="1.0" encoding="UTF-8"?>
<!--
    The plugin cannot be tested standalone: it resolves App\* classes from the
    panel it is installed into. Run this from the panel root, with the plugin at
    plugins/pouch:

      vendor/bin/phpunit -c plugins/pouch/phpunit.xml

    The php block mirrors the panel's own phpunit.xml — the tests boot the same
    application.
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="../../vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Pouch">
            <directory>./tests/Integration</directory>
        </testsuite>
    </testsuites>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="APP_URL" value="http://localhost/" force="true"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value="testing.sqlite"/>
        <env name="GUZZLE_CONNECT_TIMEOUT" value="1"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
